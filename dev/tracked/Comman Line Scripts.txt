






Symfony Make SQL Create DB Script from Entities
php bin/console doctrine:
alias dump-sql='php bin/console doctrine:schema:create --dump-sql'
dump-sql
php bin/console doctrine:schema:create --dump-sql



Symfony load data fixtures.
php bin/console doctrine:fixtures:load  --append



Update the Autoload for the class maps.
composer dump-autoload --optimize  --classmap-authoritative




DOCKER DOCKER DOCKER
DOCKER DOCKER DOCKER
DOCKER DOCKER DOCKER
DOCKER DOCKER DOCKER
DOCKER DOCKER DOCKER


3.4.1. Stop all container
docker-compose stop

docker-compose rm -f

3.4.2. Restart all container
docker-compose stop;
docker-compose rm -f;
docker-compose up -d httpd php mysql memcd;


docker-compose up -d  php mysql memcd;
.\shell.bat;

docker-compose rm -f;
docker-compose up -d php bind httpd mysql memcd redis blackfire;





exit;
docker-compose stop;
docker-compose rm -f;


docker-compose up -d  php mysql memcd;
.\shell.bat;








