<fieldset>
    <legend class="legend">{{ Legend|default('Community Details') }}</legend>
    {{ form_row(form.associationSize.numberOfProperties) }}
    {{ form_row(form.associationSize.numberOfSections) }}
</fieldset>
