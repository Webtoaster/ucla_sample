<fieldset>
    <legend class="legend">{{ nameLegend|default('Your Name') }}</legend>
    {{ form_row(form.name.nameFirst) }}
    {{ form_row(form.name.nameMiddle) }}
    {{ form_row(form.name.nameLast) }}
</fieldset>