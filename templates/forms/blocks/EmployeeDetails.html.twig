<fieldset>
    <legend class="legend">{{ Legend|default('Employee Details') }}</legend>
    {{ form_row(form.staff.jobTitle) }}
    {{ form_row(form.staff.dateStart) }}
    {{ form_row(form.staff.dateEnd) }}
    {{ form_row(form.staff.isBoardMember) }}
    {{ form_row(form.staff.isAttorney) }}
</fieldset>
