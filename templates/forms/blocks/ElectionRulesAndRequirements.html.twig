<fieldset>
    <legend class="legend">{{ Legend|default('Election Rules and Requirements') }}</legend>
    {{ form_row(form.votesMax) }}
    {{ form_row(form.voterMinPercent) }}
    {{ form_row(form.votesMin) }}
</fieldset>
