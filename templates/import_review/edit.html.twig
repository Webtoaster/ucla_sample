{% extends 'base.html.twig' %}

{% if title is not defined %}
    {% set page_title = 'DEFINE_BETTER' %}
{% else %}
    {% set page_title = title %}
{% endif %}

{% block title %}{{ parent() }} - {{ page_title }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {#	{{ encore_entry_link_tags('') }}#}
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <h2 class="h3 mb-3 font-weight-normal">{{ page_title }}</h2>
            </div>
        </div>
    </div>
    <h2>Edit Imported Property</h2>

    {#	<div>#}
    {#		{% if import.nameFormal is not empty %}{{ import.nameFormal }}<br/>{% endif %}#}
    {#		{% if import.nameFirst is not empty %}{{ import.nameFirst }} {% endif %}#}
    {#		{% if import.nameMiddle is not empty %}{{ import.nameMiddle }} {% endif %}#}
    {#		{% if import.nameLast is not empty %}{{ import.nameLast }} {% endif %}#}
    {#	</div>#}
    {#	#}
    {#	<div class="clearfix"></div>#}


    {{ include('forms/ImportProperty.html.twig') }}


    <a href="{{ path('import_index', {'upload_id': upload_id}) }}"
       class="btn btn-info btn-lg btn-block active" role="button" aria-disabled="true">Return to List</a>




    {#    {{ include('import/_delete_form.html.twig') }}#}



{% endblock %}
